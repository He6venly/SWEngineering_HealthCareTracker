spring.application.name=CWNU14_HealthCare
spring.data.mongodb.uri=${MONGODB_URI}
jwt.secret=${JWT_SECRET:local-development-secret-key}
jwt.validity-in-seconds=${JWT_VALIDITY_IN_SECONDS:86400}

# MongoDB Atlas Cloud Connection Setup
spring.data.mongodb.uri=mongodb+srv://${MONGODB_USERNAME}:${MONGODB_PASSWORD}@${MONGODB_CLUSTER}/${MONGODB_DATABASE}?appName=HealthTracker
spring.mongodb.uri=mongodb+srv://${MONGODB_USERNAME}:${MONGODB_PASSWORD}@${MONGODB_CLUSTER}/${MONGODB_DATABASE}?appName=HealthTracker
